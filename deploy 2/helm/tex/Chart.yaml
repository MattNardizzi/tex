apiVersion: v2
name: tex
description: >-
  Tex — a witness with authority. Installs the PDP (decision), the PEP
  (enforcement: kernel-floor DaemonSet + auto-injected sidecars), and the
  operator that auto-enrolls any namespace labeled tex.systems/govern=enabled.
type: application
version: 0.1.0
appVersion: "1.0"
keywords: [ai-governance, agent-security, enforcement, ebpf, admission-webhook]
home: https://tex.systems
