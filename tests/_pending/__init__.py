"""
Tests for parked packages under src/tex/_pending/.

These tests run if pytest discovers them but they target code that is
not part of the active product. Restore alongside the source when a
pending package is reactivated.
"""
