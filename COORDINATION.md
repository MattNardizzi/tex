# COORDINATION.md — running 5–6 threads in parallel without breaking the wiring

Goal: parallelize the `ROADMAP.md` build across many Claude threads with **zero file collisions** and a **green `main`** at all times.

## The mechanism: one worktree + one branch per thread

A **git worktree** is a second working folder backed by the same repo, checked out to its own branch. Threads in
different worktrees CANNOT overwrite each other — they edit different folders, merge through git.

Set up (run once, from `~/dev/tex`):
```bash
cd ~/dev/tex
git worktree add ../tex-unblock   -b track/unblock     # Wave 0: CI + auth + cut nanozk
git worktree add ../tex-abstain   -b track/abstain     # abstain boundary
git worktree add ../tex-truth     -b track/truth       # e-value spine + sealed truth object
git worktree add ../tex-struct    -b track/struct      # structural floor upgrades
git worktree add ../tex-durable   -b track/durable     # durability / Postgres / deploy
git worktree add ../tex-voice     -b track/voice       # spoken-voice loop (+ tex-systems)
git worktree add ../tex-proof     -b track/proof       # validation harness + demos
```
Then **each thread connects to its OWN folder** (`~/dev/tex-abstain`, etc.) — in the Claude app, point the new chat at
that folder. Remove a finished worktree with `git worktree remove ../tex-<name>`.

## Wave order (respect dependencies)

**Wave 0 — Unblockers (land FIRST, fast). `track/unblock`** — can itself be 2 threads (CI/auth vs nanozk) since files are disjoint:
- CI (`.github/`), close 4 no-auth routes, fail-closed auth, CORS — then **cut `nanozk`**.
- Everything else waits until CI is green and nanozk is gone.

**Early interface PR — `track/truth` ships the `TexEvidence`/e-value type FIRST** (tiny PR), because other tracks code against it. Do this before Wave 1 fans out.

**Wave 1 — Parallel build tracks (after Wave 0 + the e-value interface merge):** run all of these at once.

## File-ownership map (DO NOT edit outside your track's paths)

| Track | Folder | Owns these paths |
|---|---|---|
| **unblock** | `tex-unblock` | `.github/`, `api/auth.py`, `api/rate_limit.py`, `api/{ecosystem_twin,tee,vet,zkprov}_routes.py`, `nanozk/` (delete), CORS line in `main.py` |
| **abstain** | `tex-abstain` | `engine/crc_gate.py`, `engine/hold.py`, `learning/ope.py`, `learning/drift.py`, `learning/calibrator.py`, verdict rule in `engine/router.py` |
| **truth** | `tex-truth` | `provenance/ledger.py`, new `domain/evidence.py` (TexEvidence/e-value), `drift/_anytime_valid.py`, evidence-bundle export |
| **struct** | `tex-struct` | `systemic/probguard.py`, `specialists/structural_floor.py`, `camel/capability.py`, `governance/path_policy/ltlf.py`, contracts |
| **durable** | `tex-durable` | `stores/`, `db/`, `memory/`, `deploy/`, `render.yaml`, `Dockerfile` |
| **voice** | `tex-voice` | `api/voice_routes.py` (new: `/v1/ask`, `/v1/speak`, `/v1/voice/token`), the STT/TTS gateway, **`~/dev/tex-systems`** |
| **proof** | `tex-proof` | `bench/`, `adversarial/`, test harnesses, demo scripts |

## Hot/shared files — serialized, never edited in parallel
`engine/pdp.py` and `main.py` (`build_app` wiring) are the integration points multiple tracks need.
**Rule:** a track that needs a `pdp.py`/`main.py` change (a) keeps it MINIMAL (just wire in its new function/signal),
(b) posts a line in the Status table below, (c) merges that small change FAST, (d) everyone else rebases. Never let two
branches sit on divergent `pdp.py` edits. New capabilities should be a self-contained module that `pdp.py` calls via a
stable function — so the `pdp.py` delta is one or two lines.

## Merge protocol (keeps main green)
1. Start each work session: `git pull origin main` into your worktree (rebase or merge main in).
2. Build + **run tests** before any commit. Keep your branch green.
3. Push your branch → open a PR → CI must pass → merge to `main`.
4. **Small, frequent merges** beat big-bang. Rebase onto `main` at least daily.
5. End-of-day integration pass: merge everything green; resolve any `pdp.py`/`main.py` wiring in one short serialized step.

## Status table (each thread updates its row)
| Track | Branch | Owner/thread | Status | Touches pdp.py/main.py? |
|---|---|---|---|---|
| unblock | track/unblock | unblock thread | Wave 0 done: CI workflow + auth on 4 routers + CORS lockdown; PR open. nanozk cut still pending. | CORS line only (1 call → tex.api.cors) |
| abstain | track/abstain | abstain thread | **MERGED (#14)**: act-vs-ask boundary — WSR betting CS (ope.py) + LTT joint two-sided cert/ε-collar/SCRC (crc_gate.py) + R0–R4 unified rule (router.py) + e-detector (learning/drift.py). All behind behavior-preserving defaults; guard suite + monotone-lowering FAIL-test green. | **no** — CRC gate + router already wired in pdp.py; new params default to current behavior, so 0 hot-file edits |
| truth | track/truth | truth thread | **All 5 PRs merged** (#5 TexEvidence type, #6 e-value spine, #9 SealedFacts/PCVR ledger, #11 drift e-process wiring, #13 offline bundle+standalone verifier). Additive only; 65 truth-track + 5 guard tests green. NOT YET WIRED into the live PDP verdict — that 1–2 line `pdp.py` call belongs to the engine/abstain track. | no |
| struct | track/struct | struct thread | **MERGED (#17)**: Pro2Guard predictive ABSTAIN dim + RV4 four-valued LTLf (perm→FORBID / recoverable→ABSTAIN) + FIDES dual-axis camel lattice + Rule-of-Two contract + CaMeL-denial→FORBID. All in owned files (probguard.py, structural_floor.py, capability.py, ltlf.py, contracts/{rule_of_two,rv4_path}.py). | yes — 2 additive lines: `detect_structural_floor(.., request=request)` + `apply_predictive_holds(..)` on the routed branch; +1 import |
| durable | track/durable | durable thread | **MERGED (#7)**: Postgres write-through default-on (+ DB-gated restart-survival test); Helm replicas 2→1 + Recreate + evidence/keys PVC + DATABASE_URL/prod-secret env + TEX_ENV→TEX_APP_ENV fix + probes + scrape; real Dockerfile + .dockerignore; render.yaml web svc + Postgres + disk; .env.example; live OpenMetrics /metrics (+ optional OTLP). | **1 line**: `install_metrics(app)` in create_app (self-contained `observability/metrics.py`) |
| voice | track/voice | voice thread | **MERGED (#10)**: spoken-voice loop — `api/voice_routes.py` (/v1/voice/token, /v1/ask, /v1/speak) + `voice/` (deterministic verbalizer + exact-match faithfulness gate + ECDSA voice-attestation chain) + `gateway/` (self-hosted STT/TTS, offline backend; neural seams labeled). 40 voice/gateway tests green; verdict-path suites still green. tex-systems frontend on-contract. | 1 line (include_router) |
| proof | track/proof | proof thread | **MERGED (#8)**: Wave G — adaptive red-team (attacker-moves-second) → real ASR (static 0% / adaptive 80% / lexical 100% / **structural 0%**), sealed into ECDSA-signed offline-verifiable bundles; Replay Trial + honest-decline demos; CLI no-op gate bug fixed. Gate runs in existing pytest CI. | no |
| pow-admission | pow/admission | POW-ADMISSION thread (born-in-a-box deny-half) | **BUILT, NOT merged** (`production` deny-half / VAP / cosign; `research`+`RUNTIME-DEPENDENT` node-root): the operator gained the DENY twin it was missing (`build_admission_response` returned `allowed:True` ALWAYS). New keyword-only `mode="validate"` delegates to a pure exported `admission_violations(pod, cfg)`: denies an in-scope governed-namespace pod (not opted out via `tex.systems/govern-exclude`) that lacks an approved sandbox `runtimeClassName`, lacks `tex.systems/injected=true` (injector runs before validation ⇒ absence = injector down ⇒ fail closed), or requests host escapes (`hostNetwork`/`hostPID`/`hostIPC`/`privileged`/`allowPrivilegeEscalation`). Mutate path byte-identical (regression-pinned); `/validate` route fail-closed on unparseable body; `TEX_APPROVED_RUNTIME_CLASSES` env wired. Helm: `ValidatingAdmissionPolicy`+`Binding` (CEL, `failurePolicy: Fail`, default ON, no external dep; mirrors `admission_violations` 1:1) · optional `ValidatingWebhookConfiguration` (`/validate`, default OFF) · cosign image gating (`imagePolicy`, Kyverno `verifyImages` **or** sigstore `ClusterImagePolicy`, default OFF, render-fails without a trusted identity) · upgraded kernel-floor DaemonSet to the canonical orig-dst-UDS contract + framed as the apiserver-bypass floor. **Earned:** 35 deny-logic tests green (incl. the trap — Tex's OWN injected pod with NET_ADMIN/NET_RAW caps ADMITS); rendered CEL cross-checked EQUIVALENT to `admission_violations` over 13 pod fixtures via cel-python (caught a real bug: the `approved` var rendered as a YAML list, not a string — would have failed apiserver schema validation); `helm template` renders + `safe_load_all`-parses across 6 value combos; `helm lint` clean. Incidental fix: pre-existing `namespace.yaml` flow-map `nindent` bug that broke `helm template` entirely. **Honest boundary:** admission only sees apiserver-routed pods — static pods / direct CRI / compromised kubelet bypass it (eBPF floor redirects their egress anyway); the genuine node-root (DICE/TPM measured boot + SPIRE/Keylime + Confidential Containers) is research / RUNTIME-DEPENDENT (Tex attestation verifier-only); `c2pa/cosign_verifier.py` does C2PA-assertion, NOT OCI-image, gating — image gating is the NEW sigstore integration. cel-python ≠ apiserver cel-go (exact semantics cluster-RUNTIME-DEPENDENT). | **no** (operator/** + deploy/helm/tex/** only; pdp.py/main.py untouched) |

## Cross-track notes (struct → others)

**New opt-in `request.metadata` keys (all zero-cost no-ops when absent):**
- `systemic_lookahead` → Pro2Guard DTMC predictive ABSTAIN (`systemic/probguard.py`).
- `rv4_path_policies` → four-valued RV4 path policies; permanent→FORBID, recoverable→ABSTAIN (`contracts/rv4_path.py`).
- `rule_of_two` → untrusted ∧ sensitive ∧ state-change → FORBID (`contracts/rule_of_two.py`).

**For the `abstain` track (owns `engine/hold.py`):** the predictive holds raise two
uncertainty flags that `hold._FLAG_PIVOTS` has no tailored pivot for yet —
`systemic_lookahead_risk` (aleatoric: a forward-looking probability → HUMAN_JUDGMENT)
and `rv4_recoverable_violation` (epistemic: a pending future step would resolve it →
SELF_HEAL/HUMAN_FACT). `build_hold` degrades gracefully today (verdict is still ABSTAIN
and a hold is still built); adding those two pivots would give each a precise resolving
question. Non-blocking.

---

# WAVE 2 — Mythos tier track breakdown (the only forward priority)

Wave 2 builds the one never-built **capstone** verdict object (see `ROADMAP.md` → THE CAPSTONE): a single PERMIT/ABSTAIN/FORBID
that is simultaneously ZK-proof-carrying, hardware-attested-to-policy, post-quantum-signed, anytime-valid, reversibility-floored,
negative-knowledge-bearing, inter-org-witnessed, and self-governed. The twelve leaps are its load-bearing pieces. Same
mechanism as Waves 0–1: **one worktree + one branch per track, zero file collisions, green `main` always.** Every leap is a
**self-contained new module** that `pdp.py`/`main.py` calls in **1–2 additive lines** — no two tracks edit a hot file on
divergent lines. Every leap **fails closed to today's verdict** when its backend/corpus/seam is absent (inert certificate,
ABSTAIN-only surface, monotone-lowering preserved). The four non-negotiables (ABSTAIN-only surface · monotone-lowering ·
fail-closed · zero fabrication) are invariants no track may weaken.

## The one hard dependency — land FIRST
**`wave2-seam` (M0)** is a hard prerequisite for **L1, L3, L6, L9, L12.** (L7 was listed here pre-build, but
as shipped it consumes no M0 DECISION seam — it extends the campaign evidence chain via `_seal_one`; only a
deferred live-ledger L7 variant would need M0.) Today `SealedFactLedger`
(`provenance/ledger.py`) is *tested-but-dead* and `engine/pdp.py` appends **no** `DECISION` SealedFact — so every "seal the
verdict, then prove a property of it" leap is built on a leaf that is never produced on the live path. M0 instantiates the
ledger on the live runtime and appends one canonical `SealedFact(DECISION)` per verdict (the constitution's 1–2 additive
`pdp.py` lines into a self-contained module). **No Wave-2 leap that consumes a sealed decision may claim its `pdp.py` wiring is
"one line" until M0 merges** — several designs asserted a seam that does not exist yet.

## Worktree / branch setup (run once, from `~/dev/tex`)
```bash
git worktree add ../tex-w2-seam        -b track/wave2-seam        # M0: DECISION-sealing + fail-closed backend probes
git worktree add ../tex-w2-corpus      -b track/wave2-corpus      # M0b: labelled-corpus harness + sealed provenance (split out of wave2-seam)
git worktree add ../tex-w2-probes      -b track/wave2-probes      # M0c: fail-closed RUNTIME-DEPENDENT backend probes (split out of wave2-seam)
git worktree add ../tex-w2-actionclass -b track/wave2-actionclass # L4  (mythos-now)
git worktree add ../tex-w2-spine       -b track/wave2-spine       # L9
git worktree add ../tex-w2-pqlive      -b track/wave2-pqlive      # L10
git worktree add ../tex-w2-poguard     -b track/wave2-poguard     # L2
git worktree add ../tex-w2-zkpdp       -b track/wave2-zkpdp       # L1
git worktree add ../tex-w2-advcomplete -b track/wave2-advcomplete # L7
git worktree add ../tex-w2-credal      -b track/wave2-credal      # L8
git worktree add ../tex-w2-vcert       -b track/wave2-vcert       # L12
git worktree add ../tex-w2-reflexive   -b track/wave2-reflexive   # L5
git worktree add ../tex-w2-interchange -b track/wave2-interchange # L6
git worktree add ../tex-w2-negknow     -b track/wave2-negknow     # L3  (research-grade)
git worktree add ../tex-w2-spokenproof -b track/wave2-spokenproof # L11 (research-grade)
```

## File-ownership map (DO NOT edit outside your track's paths)

| Track | Leap | Owns these paths (new unless noted) | `pdp.py`/`main.py` delta |
|---|---|---|---|
| **wave2-seam** | M0 | `engine/pdp.py` DECISION-seal call (owns the shared seam), `provenance/ledger.py` live-instantiation wiring. (`pqcrypto/_backend_probe.py` + `tee/_mode_probe.py` moved to **wave2-probes** — the M0c backlog item was executed there; `bench/wave2_corpus/` moved to **wave2-corpus** — the M0b backlog item was executed there. The ATTEMPT-hook backlog item was executed on **track/wave2-attempthook** and is **MERGED → main (40df280, final integration 2026-06-11)**: `provenance/attempt_seal.py` + `SealedFactKind.ATTEMPT` (models.py) + the entry-seal call — same seam ownership.) | **owns** the 1–2 line DECISION-seal AND the 2-line ATTEMPT entry-seal in `pdp.py` — everyone else rebases onto them |
| **wave2-corpus** | M0b | `bench/wave2_corpus/` (labelled-corpus harness: builders, sealed provenance, kind-gated loaders, L12 field-trial entry point), `tests/bench/test_wave2_corpus.py` | **0 lines** (pdp.py/main.py untouched) |
| **wave2-probes** | M0c | `pqcrypto/_backend_probe.py` + `tee/_mode_probe.py` (fail-closed RUNTIME-DEPENDENT probes — the **wave2-seam** backlog item, executed here), `tests/pqcrypto/test_backend_probe.py`, `tests/tee/test_mode_probe.py` | **0 lines** (pdp.py/main.py untouched) |
| **wave2-actionclass** | L4 | `contracts/action_class.py` (mirrors `rule_of_two.py`/`rv4_path.py`), `tests/test_action_class.py` | 1 line in `detect_structural_floor` (opt-in via `request.metadata['action_class']`) |
| **wave2-spine** | L9 | `engine/risk_spine.py`, `tests/test_risk_spine.py` (reuses `drift/_anytime_valid.py` verbatim) | 1 line: `risk_spine.apply(...)` monotone-lowering hook on the routed branch |
| **wave2-pqlive** | L10 | `pqcrypto/pq_durability.py`, `tests/test_pq_durability.py` (reads `ml_dsa.active_backend_id()`, composite via `algorithm_agility`) | 1 line: emit `PQ-durable` SealedFact + maturity→ABSTAIN signal |
| **wave2-poguard** | L2 | `tee/verdict_binding.py`, `tests/tee/test_verdict_binding.py` (mirrors `decision_bound_nonce`; **fix verifier to check TDX `user_data`, not `eat_nonce`**) | 1 line at the `commands/evaluate_action` seam (test-mode) |
| **wave2-zkpdp** | L1 | `zkpdp/arbiter.py` (reuses `zkprov/backends.py` dispatcher + hard-gate), `tests/zkpdp/` | 0 lines (consumes M0's sealed decision; opt-in proof emission) |
| **wave2-advcomplete** | L7 | `adversarial/completeness.py`, `tests/adversarial/test_completeness.py` (drives `adversarial/adaptive.py`; binary martingale sibling of `drift/_anytime_valid.py`) | 0 lines (Layer-4/5 eval tooling, off the per-request path) |
| **wave2-credal** | L8 | `engine/credal_hold.py`, `tests/test_credal_hold.py`; **+ refactor `engine/router.py:_compute_confidence`** to emit per-stream confidences (coordinate w/ `abstain`-track owner of `router.py` verdict rule) | 1–2 lines threading per-stream confidences into `build_hold` |
| **wave2-vcert** | L12 | `engine/verdict_certificate.py`, `tests/test_verdict_certificate.py`; upgrades `bench/replay_trial.py` (robustness half) | 0–1 line (opt-in cert emission; QIF half ships as labeled point-estimate only) |
| **wave2-reflexive** | L5 | `selfgov/governor.py`, `specialists/metaguard.py`, `tests/test_reflexive_gov.py`; **must enumerate the FULL mutation surface** (`commands/{activate,calibrate}_policy.py`, `governance/standing.py`, `feedback_loop.py:669/:721`, `memory/system.py:337`, `policy_snapshot_store.py:157`, key rotation) | 1 line: route controller mutations through `gate_controller_mutation` |
| **wave2-interchange** | L6 | `interchange/gix.py`, `interchange/gix_witness.py`, `interchange/gix_merge.py`, `tests/interchange/` (RFC-9162 Merkle over `record_hash`; C2SP witness semantics in-tree, live OmniWitness behind a flag) | 1 line (publish-checkpoint hook); **hard-gated on M0** |
| **wave2-negknow** | L3 | `evidence/negative_knowledge.py`, `tests/test_negative_knowledge.py` (sorted accumulator reuses `zkprov/commitment.py`); **crypto half built; completeness UNBLOCKED 2026-06-11 — the attempt hook LANDED (track/wave2-attempthook, seam-owned `provenance/attempt_seal.py`): `check_count_conservation` derives `n_attempts` from sealed ATTEMPT facts, certs carry hook-present/complete on EPOCH-level evidence only (conservation dimension, one-sided)** | 0 lines (the hook's pdp lines are seam-owned) |
| **wave2-spokenproof** | L11 | `voice/entailment_cert.py`, `tests/voice/test_entailment_cert.py`; ship the **seal half now** (commit `model_id` + **λ̂-ABSENCE** — `lambda_hat: None`/`calibrated: Literal[False]`, a λ̂ number is fabricated calibration while no scorer emits scores — + M0b manifest digest into the ECDSA voice chain), entailment half research-grade until `transformers`/`torch`/GPU land | 0 lines (sits BESIDE the `voice/voice_gate.py` NLI seam; live wiring = 1 voice-track line on `voice_ask._gate_summary`'s `gate` dict) |
| **wave2-capstone** | CAPSTONE | `capstone/` (new package — `manifest.py` sealed manifest-of-digests `CapstoneVerdict`, `compose.py` module-verifier-delegating composer, `verify.py` offline verifier `verify_capstone(bundle_dir, pins)`, `flow.py` epoch driver, `tamper.py` adversary matrix), `tests/capstone/`, `scripts/capstone_demo.py`. (This row establishes the capstone's footprint — no module path or owner existed before this thread.) Consumer-only enumeration added to `tests/test_decision_fact_contract.py` census (4 consumer files, 0 new DECISION producers; its one new fact is `SealedFactKind.ANSWER`). | **0 lines** (pdp.py/main.py untouched — pure composition over the existing seams: `decision_ledger`/`risk_spine` ctor kwargs + the opt-in env flags) |

## Hot/shared files — serialized, never edited in parallel
As in Waves 0–1: `engine/pdp.py` and `main.py` are integration points. **`wave2-seam` owns the DECISION-seal `pdp.py` line;**
all other tracks keep their `pdp.py`/`main.py` touch to a single additive call into their self-contained module and rebase
onto `wave2-seam` once it lands. **`engine/router.py`** is shared between `wave2-credal` (the `_compute_confidence` refactor)
and the existing `abstain` track's verdict rule — serialize that one refactor through a small fast PR before `wave2-credal`
fans out. Never let two branches sit on divergent `pdp.py`/`router.py` edits.

## Suggested order (mirrors `ROADMAP.md` → Build order)
1. **wave2-seam (M0)** — unblocks six leaps; nothing sealed-decision-dependent is honest until it merges.
2. **First-green on-ramps:** wave2-spine (L9) · wave2-actionclass (L4) · wave2-pqlive (L10) · wave2-poguard (L2).
3. **Frontier certificates:** wave2-zkpdp (L1) · wave2-advcomplete (L7) · wave2-credal (L8, after the `router.py` refactor) · wave2-vcert (L12 robustness half).
4. **Reflexive + interchange:** wave2-reflexive (L5, after the full chokepoint enumeration) · wave2-interchange (L6).
5. **Research-grade (scope as projects, fail closed):** wave2-negknow (L3) · wave2-spokenproof (L11, entailment half) · wave2-vcert QIF half (labeled point-estimate only).
6. **Capstone composition** — once ≥6 of the eight capstone properties are green, compose them onto one sealed verdict object + ship the replay-proof demo.

## Status table (each Wave-2 thread updates its row)
| Track | Branch | Leap | Status | Touches pdp.py/main.py? |
|---|---|---|---|---|
| wave2-seam | track/wave2-seam | M0 | **MERGED → main (#19, ba44cd3)**: DECISION-sealing seam — `provenance/decision_seal.py`, optional `decision_ledger` on the PDP (default `None`), opt-in `TEX_SEAL_DECISIONS=1`; fail-closed + observation-only. Unblocked L9 (consumed live by batch-1) and L1's seal binding (batch-2). Still prereq for L3/L6 (+ a deferred live-ledger L7 variant; as-shipped L7 needs no M0). **ATTEMPT-hook backlog item MERGED → main (40df280, final integration 2026-06-11; adversarial-reviewed, combined 766-green, full suite 0 new failures vs re-derived 35b1704 baseline)**: `provenance/attempt_seal.py` seals one `SealedFact(ATTEMPT)` at evaluate() entry (fail-closed mirror of `seal_decision`; 2 additive pdp.py lines; `SealedFactKind.ATTEMPT` added, six→seven). Declared contracts: gate evals COUNT as attempts (global symmetric identity); n_error derivable + ONE-SIDED (error := attempt w/o verdict-keyed DECISION — omission and mid-pipeline death indistinguishable, fail-closed). Doubles per-request appends under `TEX_SEAL_DECISIONS=1`; ATTEMPT facts enter L6 checkpoints (`tree_size` is neither a decision nor an attempt count). Census-registered in `test_decision_fact_contract.py`; composition/seal/negknow pins recomposed consciously. | yes (owns DECISION-seal + ATTEMPT entry-seal lines; landed) |
| wave2-corpus | track/wave2-corpus | M0b | **MERGED → main (d1ce83d, batch-3; adversarial-reviewed 5-lens/0-blocker, 4797-green combined suite + eight-leap e2e, 0 new failures vs 554585a)**: `bench/wave2_corpus/` — calibration-corpus harness with EARNED provenance, closing the honor-system `corpus_kind="field"` gap (action_class.py:590 string-equality gate; verdict_certificate.py:519/:563 membership-only). Sealed `CorpusProvenance` (collector/method/window/source + SHA-256 binding to exact corpus bytes) via the production signer + `verify_bundle` (the `adversarial/seal._seal_one` construction; ECDSA-P256 today); loaders emit `"field"` ONLY on verified field-collection attestation + PINNED authorship + digest binding — no pin / tamper / manifest contradiction ⇒ raise, never downgrade-to-trusted. Builders (deterministic, seeded, exact consumer types): L4 `ActionClassCase` (reuses in-tree anti-circular generator), L12 neighborhood texts + FIDES-confidentiality-tagged QIF/CRC red-team points (`secret_label` = `ConfidentialityLevel`, coupling-parametrized channel), L11 closed-world NLI pairs (entailed/not-entailed aligned to gate vocabulary sealed/unsealed/contradiction/unverifiable; positives = template-fill by construction). Separate L12 FIELD-trial entry point (`field_trial.run_field_neighborhood_trial`; refuses non-field; family from sealed provenance, never `NEIGHBORHOOD_FAMILY`; synthetic trial untouched). Adapters feed consumers an EARNED kind (`certify_action_class_corpus` w/ ≥20-miss anti-circularity tripwire, `qif_certificate_from_corpus`). **Earned:** 24 new tests — byte-flip (corpus + bundle) caught; tamper-then-resign synthetic→field upgrade passes integrity but fails the pin AND is refused unpinned; field label structurally unreachable from builder paths (no kind param + model validators + source tripwire); synthetic stays `certified=False` everywhere; field e2e vs live runtime (78/78 FORBID ⇒ stored p_low 0.950062 ≥ 0.95 ⇒ certified=True through the earned label, SIMULATED-field fixture); gate arithmetic pinned to closed form `(δ/e)^(1/n)` (n=78 min @ α=δ=0.05; CP sizing ref 59). 198 verdict-path spec green; full suite 0 new failures (21 = exactly the pre-existing set). Honest residuals: the seal makes a false field attestation ATTRIBUTABLE + tamper-evident, not impossible (key-holder can still lie — same residual as every provenance system); **real field-data collection is BLOCKED on real-world inputs** — every artifact buildable today is synthetic/`certified=False`, which is the correct state. `research-solid` (crypto production, discipline newly wired). | **0 lines** (pdp.py/main.py untouched) |
| w3-l4l12-certify | track/w3-l4l12-certify | L4+L12 | **Built, NOT merged** (`research-solid` path; certificates stay `certified=False` — genuinely BLOCKED on real field-data collection, not on code): completes the field-corpus certification path so that GIVEN a sealed field corpus the L4 under-classification and L12 robustness certificates read `certified=True` and re-check offline. L12 robustness already reached this via the simulated-field e2e (`field_trial.run_field_neighborhood_trial`, 78/78 FORBID ⇒ stored p_low 0.950062); this track adds the missing **L4** analog. New: `contracts/action_class.build_certifiable_action_class_corpus` (shares `_sample_action_class_case` with the existing builder VERBATIM — ONE truth model; default `build_action_class_corpus()` byte-identical, fingerprint-verified for seeds 1729/42/20260601) — a LOW mis-declaration rate (`p_under=0.055`) over a LARGE holdout (`n_cal=500`/`n_hold=2000`) so BOTH L4 gates clear at once: the calibration Hoeffding-Bentkus UCB ≤ α (0.0269) AND the anti-vacuity tripwire's ≥20 genuine holdout misses (44). `loaders.certify_action_class_from_artifact` — the L4 offline re-verification entry (`load_corpus` re-verifies sealed provenance + re-derives the certificate, fully deterministic, no runtime). **Earned:** L4 SIMULATED-field dry run (attest→seal→load(gate)→certify ⇒ certified=True through the EARNED label; offline re-check byte-identical; unpinned field claim refused; same bytes synthetic ⇒ certified=False) + contract-level certify/determinism/tripwire tests; 99 named-file tests green; capstone demo exit 0, offline VALID 34/34, **honesty pins UNTOUCHED** (L4.certificate/L12.robustness stay `uncertified`, L12.qif `estimate_only`; the `_l4_certified`/`_l12_certified` mutations stay unconstructible). Full suite 0 new failures (24 deterministic pre-existing on a clean-tree re-run — durable ML-DSA backend present flips the "no-backend" PQ/route tests + the env-dependent twelveleap counts — plus 1 causal-p99 perf flake). `NOTES.md` specifies the exact corpus to certify (L4: ≥500 cal / ≥2000 holdout, under-rate ≤~0.02, ≥20 holdout misses, labels = declared steps + independent must-FORBID truth; L12: n≥78 zero-instability) and why the capstone stays uncertified until real data. Wiring a *verified* field cert into the capstone manifest is deliberate FUTURE work (would make `_enforce_honesty_pins` conditional, never an unconditional flip). | **0 lines** (pdp.py/main.py untouched; one shared-helper refactor in `contracts/action_class.py`, runtime classify path unchanged) |
| wave2-probes | track/wave2-probes | M0c | **MERGED → main (40df280, final integration 2026-06-11; adversarial-reviewed, combined 766-green, full suite 0 new failures vs re-derived 35b1704 baseline)** (`research-solid`): `pqcrypto/_backend_probe.py` + `tee/_mode_probe.py` — four fail-closed RUNTIME-DEPENDENT presence probes (ezkl/Halo2-KZG L1 · signer ML-DSA maturity L10 · torch+NLI+GPU L11 · confidential-VM/TDX L2), executing the wave2-seam backlog item. Shared frozen `ProbeResult` construction-coherent (available ⇔ tier>floor ⇔ missing empty) + one outer never-raise guard; closed-world (absent/unrecognized/raising ⇒ unavailable + NAMED missing; env vars can redirect where a check looks — ezkl's own `EZKL_REPO_PATH`, verified vs zkonduit/ezkl src/execute.rs 2026-06-11 — never assert availability). Delegation not duplication: L10 tier = `pq_durability.probe_backend()` verbatim (divergence-pinned), OpenSSL CLI a separate NON-contributing fact (nanozk trap re-pinned at probe layer); L11 available requires `NeuralNLIScorer.load()` verified-load (imports+GPU alone pinned insufficient; neural path untouched, off-and-honest pin re-asserted); L2 mirrored components ∧ `is_tdx_capable` oracle conjunction (drift can only under-report) + source-literal pin; `TEX_TEE_ATTESTATION_MODE` reported never consulted (production-on-a-laptop pinned honest). **Probe ≠ trust earned:** all four monkeypatched available=True ⇒ PDP evaluate byte-identical (volatile fields verified pre-patch: decision_id/timestamps/latency/evidence_hash only); source-scan tripwire = ZERO live consumers (a leap wiring a probe must consciously extend the allowlist); no foreign env flag flips a probe. 74 tests (8/16/8-cell truth tables; exploding/garbage checks → coherent result); battery 324 passed/11 failed = exactly the pre-existing blake3 set; full suite 4878/21 vs re-derived 35b1704 baseline 4803/23 = **0 new failures** (Δ = the 2 documented causal-p99 flakes passing this run). NO consumer wiring — consumption is L1/L10/L11/L2's own future opt-in. | **0 lines** (pdp.py/main.py untouched) |
| wave2-actionclass | track/wave2-actionclass | L4 | **MERGED → main (b47d9ae, batch-1; adversarial-reviewed, 236-green, 0 new failures)**: `contracts/action_class.py` — reversibility×blast IntEnum join-semilattice (UNKNOWN fail-closed tops, worst-step join), fixed FORBID/ABSTAIN/NEUTRAL cell map, opt-in `metadata['action_class']` (no-op absent; reads no envelope), single-hypothesis Hoeffding-Bentkus UCB on the **under-classification** rate (reuses `crc_gate`; `certified=False` until a *field* corpus — synthetic computes-but-abstains), anti-circular 300-cal/200-test corpus w/ tripwire. ONE additive call in `detect_structural_floor` (FORBID cell only; ABSTAIN recorded-only, rv4-recoverable precedent). Earnable tests green: 0.9 score forbids *via the lattice* / 0.1 can't silence / spoofed bundle can't fire. 111 passed (named verdict-path set + new). | **no** — 1 additive call in `detect_structural_floor`; zero pdp.py/main.py |
| wave2-spine | track/wave2-spine | L9 | **MERGED → main (b47d9ae, batch-1; adversarial-reviewed, 236-green, 0 new failures)**: `engine/risk_spine.py` — streaming `AnytimeValidEProcess` per stream_id (drift `_anytime_valid.py` reused VERBATIM) → multiplicative composite via truth-track `compose_spine`, monotone-lowering PERMIT→ABSTAIN, each step sealed `SealedFact(DRIFT)`. **Earned:** Ville false-hold 0.029≤α under continuous peeking (N=2000,T=500) at the abs-corrected `2^K/α` level (naive `1/α` over-fires at 0.059 — the `\|S_t\|` two-sided construction needs the 2^K correction); μ=0.5 drift detects 100% ≤167 steps; K=2 product 0.0185. Only drift e-processes multiplied (per-agent/CRC/OPE excluded). Inert by default (no main.py). 16 new + 72 verdict-path tests green; adversarial review: 4 non-negotiables held. | **1 line** on routed branch (`apply_risk_spine`) + self-contained-module injection mirroring M0's `_decision_ledger` |
| wave2-pqlive | track/wave2-pqlive | L10 | **MERGED → main (b47d9ae, batch-1; adversarial-reviewed, 236-green, 0 new failures)**: `pqcrypto/pq_durability.py` — fail-closed maturity probe (`active_backend_id`→DURABLE/RESEARCH_ONLY/NONE; any unrecognized id→NONE), monotone-lowering ABSTAIN signal (PQ-non-repudiation claim + non-durable signer ⇒ PERMIT→ABSTAIN, opt-in `metadata["pq_non_repudiation"]`), sealed `PQ-durable=false` fact (fail-closed), and a REAL composite ML-DSA-87+ECDSA-P384 chain-head round-trip via the OpenSSL-3.5 CLI (good verifies, 1-bit-flip fails). Nanozk trap pinned: CLI shim does NOT raise live maturity. 40 track + 138 verdict-path/M0/integration tests green; 0 new failures (21 pre-existing). | 1 line (+1 import): `apply_pq_durability_hold(...)` on the routed branch, after `apply_predictive_holds` |
| wave2-poguard | track/wave2-poguard | L2 | **MERGED → main (b47d9ae, batch-1; module only — adversarial-reviewed, 236-green, 0 new failures)**: `tee/verdict_binding.py` folds categorical verdict + policy-bundle digest + decision-input hash + prior ledger hash into the composite-attestation `report_data` (`verdict_bound_nonce`→`recompute_expected_nonce`, SHA-256[:32] mirroring `decision_bound_nonce`). **Bug fixed**: verifier gates on TDX `tdx_report_data`, NOT `eat_nonce` — `eat_nonce` demoted to a fail-closed consistency check (can only lower, never forge). 45 tests green (verdict-swap→`verdict_nonce_mismatch`, forgery=0, hollow-`eat_nonce` regression proves the fix is load-bearing); 107 existing TEE tests still green. `research-early`; report_data unforgeability is RUNTIME-DEPENDENT on real TDX. **Deferred (honest):** the 1-line `commands/evaluate_action` seam wiring waits on M0 (DECISION-sealing supplies `ledger_prev_hash` on the live path). | not yet — seam wiring deferred to post-M0 |
| wave2-zkpdp | track/wave2-zkpdp | L1 | **MERGED → main (08b3831, batch-2; adversarial-reviewed, 494-green combined suite + eight-leap e2e, 0 new failures)** (13 confirmed review findings incl. fail-open seal gating + R0 quarantine/capability precedence inversion, all fixed): `zkpdp/arbiter.py` — all-public fixed-point (10⁴) `ArbitrationStatement` + the arbitration relation (fuse→threshold→FORBID-floor→monotone gate) as **UNSAT-when-violated** (`evaluate_relation`, ~22 named constraints, each mutation-pinned); prove/verify via `zkprov` dispatcher (`allow_shim_fallback=False`), verifier-internal hard gate `TEX_ZKPDP_ALLOW_SHIM` default-deny → `zkpdp_shim_not_a_real_proof` (nanozk discipline); consumes M0 `SealedFact(DECISION)` fail-closed (not_sealed=normal; mismatch/broken-chain/bad-signature reject; authorship only with PINNED key). **Earned:** N=10k differential — honest 10000/10000 vs independent oracles, **0/10500 flipped-verdict accepts** (incl. malicious shim-key prover); live-PDP cross-check covers PERMIT/ABSTAIN/FORBID + short-circuit + deterministic-block floor + quarantine pin. Honest gaps: relation proves *derivability*, seal binds the produced verdict (chain-omission residual documented+tested); shim = keyed-hash stand-in, never a proof; real ezkl/Halo2 numbers `RUNTIME-DEPENDENT`/BLOCKED on M0c (`BackendUnavailable` pinned). `research-early`. 29 track + 138 verdict-path green; full suite 0 new failures (23 = known 21 + 2 flaky perf). | **0 lines** — pdp.py/main.py untouched |
| wave2-advcomplete | track/wave2-advcomplete | L7 | **MERGED → main (08b3831, batch-2; adversarial-reviewed, 494-green combined suite + eight-leap e2e, 0 new failures)** (4 lenses, 0 blockers): `adversarial/completeness.py` — adversary-EXPOSURE certificate (completeness never claimed; record-type token disclaimed in-band): one-sided **binary betting supermartingale** (predictable GRO plug-in bets, truncation 0.5/p0, Ville at plain `1/α` — deliberately NOT the spine's two-sided `2^K/α`), default p0=0 deterministic-floor null (one PERMIT = deterministic refutation, p→0); `residual_asr_upper` (upper-on-ASR, defense-side) reuses `wsr_upper_bound` VERBATIM; sealed first-class coverage block (seeds/classes/budget/spent/min-queries/10 operators/attacker-class/rng_seed — vacuous certs refused, not sealed); `completeness_certificate` record chained via `_seal_one` onto `seal_campaign`. **Earned:** holds-run 1800q/0-breach/p=1 exactly (capital stays 1 — zero evidence, honestly), injected-breach fires (p=0 at first PERMIT), MC false-fire 0.0165≤α (N=2000,T=500, continuous peeking), tamper-resign forgery passes integrity but is caught by the PIN. 22 new + 148 verdict-path/adversarial tests green; 0 new failures. As shipped consumes NO M0 DECISION seam (extends the campaign evidence chain; the M0-prereq note at :100/:160 refers to a deferred live-ledger variant). `research-early`. | **0 lines** (pdp.py/main.py untouched) |
| wave2-credal | track/wave2-credal | L8 | **MERGED → main (08b3831, batch-2; adversarial-reviewed, 494-green combined suite + eight-leap e2e, 0 new failures)** (`research-early`): `engine/credal_hold.py` — credal interval over the fusion weight-polytope (L1-ball∩simplex × confidence score-boxes; closed-form greedy LP extrema, brute-force-grid-verified) + EPIG resolver (two-point endpoint posterior, lexicographic resolve-prob/width-drop). Commit 1 (serialized per hot-file rule): `_compute_confidence` refactor behavior-preserving — fused float bit-identical, components surfaced as `conf_stream:*` in `RoutingResult.scores`; touched ONLY `_compute_confidence`+`route()` emit, `_determine_verdict`/`_should_abstain`/`SelectiveRiskRule` untouched (abstain-track region). `build_hold` +1 kwarg (default None ⇒ byte-identical) → EPIG re-ranks WHICH resolving question an ABSTAIN asks first; observation-only, fail-closed, pure. **Earned:** N=2000 one-true-pivot benchmark under LIVE point-fusion dynamics (anti-circular: resolution ≠ the credal functional): EPIG 0.2654 vs dict 0.1483 / random 0.1567 / lowest-conf 0.1779 fraction-resolved-per-question; + N=500 credal-dynamics coherence 0.7812 vs 0.3307. 118 behavior-preservation + 100 verdict-path tests green; full suite 4523 passed / 21 failed = exactly the pre-existing set (0 new). Citations primary-source-verified 2026-06-10 (EPIG arXiv:2304.08151; Levi 1980/Walley 1991; conformal-credal arXiv:2402.10723). Wired EPIG honestly ≈ weight×uncertainty + band/clamp corrections (synthetic posterior; North-Star Layer-6 posterior still open). | **1 line** (`stream_confidences=` conf_stream filter at the `build_hold` call, pdp.py:442) |
| wave2-vcert | track/wave2-vcert | L12 | **MERGED → main (08b3831, batch-2; adversarial-reviewed, 494-green combined suite + eight-leap e2e, 0 new failures)**: `engine/verdict_certificate.py` — robustness half = genuine Hoeffding–Bentkus-complement `p_low` over a SEEDED named family (`intent-preserving-paraphrase-v1`; pinned to the independent closed form: 40/40 stable @ δ=0.05 ⇒ p_low=0.904934; reuses `crc_gate.hoeffding_bentkus_ucb` verbatim, no drift); QIF half = min-entropy-leakage + Shannon-MI **plug-in point estimates** of the named `verdict_enum{3}` channel ≤ ceiling log₂3 (the word "bound" banned + test-enforced over names/descriptions/values/errors; `qif_certified: Literal[False]` — structurally unconstructible). Honesty gate hardened by 16-agent adversarial review (13 raw → 8 confirmed → fixed): `certified` requires field corpus AND δ≤α pairing AND gate-on-STORED-floored-p_low (self-consistent artifact). Inert default; cert NEVER read by verdict path (source tripwire over 5 engine files). `replay_trial.py` seeded trial ADDITIVE — fixed-10 trial + its 10/10 pins intact. Citations re-verified vs primary (Smith FoSSaCS'09; Köpf–Smith CSF'10 Cor.2; Cohen ICML'19). 31 track + 140 verdict-path green; full suite 0 new failures (22 = 21 pre-existing + 1 documented causal-p99 flake, passes isolated). `research-early`. BLOCKED/UNVERIFIED: finite-sample QIF guarantee (North-Star, future); field corpus (M0b unlanded ⇒ certified=False is the correct state). | **1 line (+1 import)**: always-on inert posture `metadata["pdp"]["verdict_certificate"]` via stable `verdict_certificate_metadata()` seam (CRC else-branch mirror; M0-seal channel rejected — invisible in default runtime) |
| wave2-reflexive | track/wave2-reflexive | L5 | **MERGED → main (d1ce83d, batch-3; adversarial-reviewed 5-lens/0-blocker, 4797-green combined suite + eight-leap e2e, 0 new failures vs 554585a)** (`research-early`): `selfgov/governor.py` + `specialists/metaguard.py` — Tex's own controller mutations routed through the SAME `PolicyDecisionPoint.evaluate` under a deploy-frozen code-constant governor policy (level-0 never enters any store → regress killed by construction), metaguard deterministic signatures composed monotone-lowering ONLY (floor→FORBID: governor-self-target / revoked-resurrection; caution→PERMIT→ABSTAIN: quarantine-lift / governance-weakening / capability-widening / key-material / evidence-destruction; scores can neither fire nor silence — pinned), every gated outcome sealed as the FIRST `SealedFact(ENFORCEMENT)` producer + DECISION via the M0 seam in ONE chain. **The leap is the enumeration:** 18 gate calls wired across 7 chokepoint files (policy stores ×2 incl. the save-replaces-active-bytes bypass + save_in_tx no-change fast pass, feedback_loop apply/rollback at METHOD level [all-or-nothing], standing [deny-by-NOT-mutating, exception-swallowing caller], agent_registry save/set_lifecycle [postgres twin covered via delegation], c2pa keys ×3, evidence `_persist_key`); census MUST/SHOULD/EXCLUDE + `DEPLOY_FROZEN_STRATUM` (env flags, key files, binding token, arbitrary-code threat model) enumerated in-module + DOUBLE tripwire (census→code gate-line check + reverse no-unenumerated-public-mutators). **Earned:** walk-down attack — the REAL learning loop (false-FORBID seeding) proposes a genuinely weakening calibration → denied all-or-nothing + sealed; weakening activation, active-byte-replacement, quarantine-lift all denied state-byte-identical; REVOKED-resurrection floors; SLEEPING→ACTIVE wake + tightening + protective lowering all PERMIT; 0 chain breaks (`verify_chain()["intact"]` / `verify_signatures()["valid"]`); no-regress (nested mutation during a gate eval denied via backstop, max eval depth == 1); unbound default byte-for-byte inert; outer apply-gate computes the exact diff the nested activation sees (source≠active half-apply hazard closed + pinned). 26 new + 135 verdict-path tests green; full suite re-run on the final tree vs same-session baseline at 554585a (21 failed / 4619 passed): 0 new failures. Citations re-verified vs arXiv abstracts 2026-06-11 (Nidus 2604.05080, Aegis 2603.16938, corrigibility 2507.20964). **Honest gaps (named):** gate is INERT until `bind_reflexive_governor` (M0 opt-in convention; no composition-root wiring this PR), PDP has no mutation-descriptor calibration (deterministic power = metaguard), census is enumerated + tripwired, NOT proven complete; North-Star "provably cannot ungovern itself" out of scope. | **0 lines** pdp.py/main.py; 1 `gate_controller_mutation(...)` call per chokepoint in the L5-enumerated files |
| wave2-interchange | track/wave2-interchange | L6 | **MERGED → main (d1ce83d, batch-3; adversarial-reviewed 5-lens/0-blocker, 4797-green combined suite + eight-leap e2e, 0 new failures vs 554585a)** (`research-early`): `interchange/gix.py` + `gix_witness.py` + `gix_merge.py` — RFC-9162 inclusion+consistency proofs over the ONE real MTH (`ecosystem/_window.py`; §2.1.3/§2.1.4 verifiers re-fetched from the RFC 2026-06-11; exhaustively cross-validated n≤33; scitt.py tree deliberately NOT reused), C2SP byte-exact wire formats (tlog-checkpoint note, signed-note Ed25519 0x01, add-checkpoint `old <size>` body, cosignature/v1 = keyID‖u64-BE-timestamp‖sig with alg byte 0x04 — all specs re-fetched 2026-06-11), full witness state machine (404/403/400/409+latest/422/200, atomic check-and-persist). **Earned:** headline non-equivocation — 4/4 (≥3) witnesses refuse forked-and-extended (422) AND same-size-rewrite (409) checkpoints signed by the REAL log key, honest extension cosigns 4/4; restart-fork refused both ways (409 from 0, 422 claiming continuity) — checkpoint continuity across restarts NOT claimed; fresh-witness-can't-see-old-forks trust boundary tested honestly. `federated` STRUCTURALLY False (EXTERNAL_FEDERATED unconstructible in-tree, L12-qif pattern; `TEX_GIX_WITNESS` flag tested ignored by verification across 5 values). Authenticated mean-merge: 3 proofs in sequence (witnessed checkpoint → record re-derivation+pinned-ECDSA → RFC-9162 inclusion), true-e-values only (abstain/no-evidence refused), disjointness guard (stream/component/combination/origin), log-mean-exp pinned to `domain/evidence`; mean-not-product per Vovk–Wang AoS 2021 + arXiv:2409.19888 (both retrieved 2026-06-11). Claim ceiling: in-tree C2SP cosigning semantics vs self-hosted witnesses — protocol logic, NOT organizational independence. 109 track tests + verdict-path 218 green; full suite 21 failed = exactly the pre-existing baseline re-derived at 554585a (0 new). | **1 line (+1 import)**: `build_gix_checkpoint_publisher(decision_ledger)` after the M0 ledger line (inert None unless `TEX_GIX_WITNESS=1` AND ledger); 0 in pdp.py/ledger.py (tripwire-tested) |
| wave2-negknow | track/wave2-negknow | L3 | **MERGED → main (d1ce83d, batch-3; adversarial-reviewed 5-lens/0-blocker, 4797-green combined suite + eight-leap e2e, 0 new failures vs 554585a)** (`research-early`): `evidence/negative_knowledge.py` — sorted-key accumulator over `payload_sha256` (keys recomputed from `fact.canonical_payload()`, never trusted from the stored field; reuses `zkprov/commitment.py` Merkle; records the hash that ACTUALLY ran, poseidon vs sha256-fallback), adjacency non-membership (boundary/interior index-authenticated inclusion pairs; soundness explicitly *conditional on* the rebuild-auditable sortedness commitment — said in the verifier's own reasons), explicit empty-epoch sentinel (never calls the empty-rejecting primitive; `vacuous=True`), count-conservation predicate (attempts == P+A+F+errors) three-valued: **UNGATED (holds=None, never a vacuous pass)** while the attempt hook is absent. **Earned:** omission attack — rebuilding the epoch minus one PERMIT breaks the gated predicate AND the rebuilt root fails `verify_epoch_commitment` against the sealed commitment (relabel-as-error variant shown gameable on counts alone and caught by the root); present-key certs refused (`KeyPresentError`) + grafted/forged certs rejected; tamper detected (sibling/index/non-adjacency/foreign-root/record_count-inflation/sortedness); the verifier REJECTS over-claiming certs (complete=True, hook-present, fabricated UNGATED pass, hidden vacuousness, cross-backend); vocabulary pin: "never saw"/"provable ignorance" never unqualified (source-scan test). 19 track tests + verdict-path spec 128 green; full suite at cut 554585a = exactly the 21 pre-existing failures (0 new). **ATTEMPT-HOOK SCOPING DELIVERED for the seam track** (in-module docstring §ATTEMPT-SEALING HOOK): seal at evaluate() entry between `pipeline_start` (pdp.py:238) and the deterministic gate (:240); reuse the `_decision_ledger` slot; 1-2 lines via `seal_attempt(...)` mirroring `seal_decision`'s fail-closed shape; requires a NEW `SealedFactKind.ATTEMPT` member (models.py:207-216 has none — detail-typing DECISION would misname a pre-verdict fact); n_error question flagged (error-outcome seal vs attempt-without-decision — seam-track call). **COMPLETENESS UNBLOCKED + LIVE ON MAIN 2026-06-11 (track/wave2-attempthook, MERGED → main 40df280):** the attempt hook landed; `ATTEMPT_HOOK_PRESENT=True` (flipped after live seam verification), `check_count_conservation` derives `n_attempts` from sealed ATTEMPT facts (supplied-count contradiction = GATED-BROKEN fabrication alarm; pre-hook epochs keep the trust-me/UNGATED paths), certs carry `attempt_hook_present`/`complete=True` on EPOCH-level evidence only — scoped to the count-conservation dimension, ONE-SIDED per the declared n_error contract; verifier both-directions (hook-present accepted with derived evidence, rejected without). The omission attack now closes end-to-end with zero trust-me inputs. Residual blind spot stands: an entry hook bounds, never eliminates, uncounted work (pre-entry deaths invisible). Commitment anchoring remains in-memory-only — re-anchoring belongs to the interchange/witness track. | **0 lines** (the hook's pdp lines are seam-owned) |
| wave2-spokenproof | track/wave2-spokenproof | L11 | **SEAL HALF MERGED → main (40df280, final integration 2026-06-11; branch head 06c7ea2, 19 tests on the merged tree; each load-bearing check mutation-verified ×5 at build; full suite 0 new failures vs re-derived 35b1704 baseline). Entailment half stays BLOCKED** (`research-grade`): `voice/entailment_cert.py` — frozen `EntailmentCommitment` (extra=forbid) sealing the scorer IDENTITY into the ECDSA voice chain, riding the free-form `gate` dict (`VoiceAttestor.seal`'s `record_type` is fixed; voice_ask's `_gate_summary` is the natural live carrier): `model_id` read from the seam's own configured default (voice_gate.py:184, identity not status — `scorer.name` stays "neural-nli(off)"), `model_loaded: Literal[False]` (load() False BY DESIGN even if transformers imports), `threshold_label` = the honest research-early constant, and **`lambda_hat: None` + `calibrated: Literal[False]` — a λ̂ number / calibrated=True / model_loaded=True / field-kind binding are all STRUCTURALLY UNCONSTRUCTIBLE** (L12 `qif_certified` pattern; M0b field-corpus rule). Rejected design (named): sealing a "synthetic λ̂" from the M0b NLI corpus — λ̂ is a quantile of nonconformity scores FROM THE SCORER IT GATES, and `NeuralNLIScorer.entails` returns None unconditionally (voice_gate.py:204-207), so any sealed float would be a statistic wearing the name (the nanozk lie in a commitment); the corpus makes the quantile computable for a FUTURE scorer only (builders.py:308-310). `calibration_manifest_sha256` binds the exact M0b corpus bytes (hash, never store); `commitment_from_corpus` takes the loader-gated `LoadedCorpus` and refuses hand-typed `kind="field"`. Verification keeps the THREE proofs separate (attestation.py:22-26): chain=integrity+ordering, signature=authorship of one act, authorship `None`=UNVERIFIED without the pinned key. **Earned:** round-trip in a mixed chain (commitment coexists with real answer seals); **model-swap-fails-replay BOTH ways** — wrong expectation ⇒ `model_id_mismatch`; ephemeral-key re-mint (attestation.py:32-36) passes self-verification but FAILS the pin with integrity-ok/authorship-failed distinguished in one result; corpus **byte-flip ⇒ manifest mismatch**; sealed-vocabulary scan ("guarantee"/"coverage"/"1-alpha" absent from everything sealed outside THRESHOLD_LABEL's pinned "no proven coverage" negation; the artifact never calls itself a certificate); standalone seal's verdict marker is outside PERMIT/ABSTAIN/FORBID (sealing a commitment can never fabricate a verdict event); untouched pins green (`test_voice_gate_exact_match.py`, `test_voice_attestation.py`). Citation re-fetched 2026-06-11: arXiv:2512.15068 "The Semantic Illusion: Certified Limits of Embedding-Based Hallucination Detection in RAG Systems" (conformal NLI/embedding detection ⇒ 100% FPR at target coverage on real RLHF hallucinations) — the verified reason this module never claims coverage. **BLOCKED/UNVERIFIED (entailment half):** `import transformers` raises in-env + no GPU (M0c owns the fail-closed probe) + a FIELD NLI corpus (M0b built the harness; real collection blocked on real-world inputs). **Deferred (honest):** live wiring = 1 voice-track line riding `_gate_summary`'s already-sealed `gate` dict (L2 seam-deferral precedent). | **0 lines** (pdp.py/main.py/voice_gate.py/voice_ask.py/attestation.py untouched) |

| wave2-capstone | track/wave2-capstone | CAPSTONE | **MERGED → main (5424b3f, capstone integration 2026-06-12; adversarial-reviewed: 42/42 capstone tests + demo exit 0 (34/34 offline checks, 11/11 tamper rows, 1054-record epoch) + 90/90 verdict-path spec re-earned on the landed tree; full suite 4953 passed / 23 failed vs re-derived origin/main baseline 4913 / 21 — failed-set diff empty after flake adjudication (the 2 known causal-p99 perf flakes, isolated-pass, 0 causal lines in the diff); all 8 recorded traps HELD with 0 findings, incl. census mutation-check (allowlist entry removed → census FAILS → restored green) and a scratch-session over-claim construction attempt rejected by pydantic ValidationError on all 4 tries)** (`research-early` — the composition is real and replayable today; promoted only as each RUNTIME-DEPENDENT half lands): `capstone/` — ONE sealed `CapstoneVerdict` manifest binds one FORBID decision's identity (request_id, content_sha256, determinism_fingerprint, verdict) to the digests + pins + module-verifier results + maturity labels of all eight properties; the manifest's sha256 is sealed as the FINAL `SealedFactKind.ANSWER` fact of the SAME epoch chain (subject `capstone:<request_id>` — can never shadow the M0 seal binding) and the post-seal head is witness-cosigned (the witnessed root covers the manifest). THREE chains, THREE pins, never spliced: SealedFactLedger (PEM), evidence campaign bundle (b64), voice attestation (b64). Offline `verify_capstone` delegates every crypto check to the owning module's verifier (no re-implementation; rehydrated `_LedgerView` feeds `verify_arbitration`'s seal binding); 34/34 checks green from files + pins alone; the L1 stand-in FAILS CLOSED without `TEX_ZKPDP_ALLOW_SHIM=1`. Honest split machine-readable and over-claims UNCONSTRUCTIBLE (pydantic validators, the L12 `Literal[False]` pattern at composition level): green_test_mode={L1,L2}, uncertified={L4.cert,L12.robustness}, estimate_only={L12.qif}, blocked={L11.entailment}, runtime_dependent={L10.pq_signing}. Tamper matrix: 11 rows each caught by the RIGHT proof (byte-flip per chain→integrity; tamper-then-resign per chain→integrity PASSES, only the pin catches; verdict swap→L2 `verdict_nonce_mismatch` AND L1 relation-UNSAT `deny_floor_requires_forbid`; rogue-operator fork→3/3 witnesses CONFLICT; epoch-minus-PERMIT→L3 GATED-BROKEN, minus-attempt-too→epoch-commitment roots refuse; artifact swap→digest binding; manifest edit→chain-sealed digest). Demo `scripts/capstone_demo.py` exit 0 (40-sample neighborhood, 60-query campaign, 1054-record epoch, conservation GATED-HOLDS at scale; replay-trial + honest-decline folded in, not regressed). 42 capstone tests; scoped gate 219 green. The one stub: the LLM-provider seam (`_PermitSemanticAnalyzer`, the twelve-leap pattern, restated in `flow.py` since tests/factories is not importable from src). | **no** — 0 lines |
| capstone-pqfix | track/capstone-pqfix | CAPSTONE follow-up | **MERGED → main (2026-06-12; integration thread — gates re-earned at a1eb3bc: capstone 51/51, demo exit 0 [34/34 offline checks, 11-row tamper matrix], contract+twelveleap 9 green, verdict-path 81 green, full-suite failed-set diff vs origin/main baseline EMPTY [21 identical pre-existing failures both sides, 4964 vs 4955 passed = +9 new tests]; adversarial review items 1–5 all HELD — incl. 3 scratch over-claim constructions rejected by the manifest coherence pin and the durable caveat verified verbatim in a durable-pinned flow)**: the PQ scenario no longer hardcodes the PQ-immature outcome — `capstone_demo.py` crashed with `AssertionError` (flow.py:285) on every fresh install because pip now resolves cryptography 48.0.1, a durable pyca ML-DSA backend (`probe_backend()`='durable' ⇒ the claim is honorable ⇒ PERMIT, no flag, no sealed fact — the ENGINE was correct in both environments; the demo's assertion was env-pinned). Fix is capstone-module-only: `flow.py` branches on the same `pq_durability.assess()` the engine consults (each env has exactly ONE accepted outcome, asserted exactly); `compose.py` composes whichever coherent outcome the epoch produced (lowered: flag+ABSTAIN+sealed pq_durable=False fact, selected by detail-shape not order; durable: PERMIT+no-flag+NO fact — the engine seals only when the signal fires, so the durable evidence is the chain-sealed manifest itself + the digest-bound decision doc) and L8's hold rides whichever ABSTAIN companion exists (decision_pq lowered / decision_drift durable, carrier named in the manifest); `manifest.py` L10 pin became a COHERENCE pin (new required `maturity_outcome` ∈ {lowered_to_abstain, durable_not_lowered}; pq_durable=True constructible ONLY as the coherent claim-honored record — durable maturity + named backend + no sealed-fact claim; pq_signing half stays runtime_dependent in BOTH branches); `verify.py` branches L10.fact / L8.hold on the manifest's recorded outcome+carrier. **Substance caveat carried in the durable manifest verbatim:** the probe reports BACKEND maturity; chain-1 seals still verify under ECDSA-P256 against the pinned key — claim_honored is the module's assessment, never a PQ property of this epoch's signatures (the probe-measures-capability-not-use gap is reported to the L10 track below). **Earned:** 51/51 capstone tests (42 existing — session fixture now PINS the immature probe so the lowered shape is deterministic on every box — + 9 new both-branch tests incl. 4 unconstructibility pins and a coherent-forged-outcome-flip caught by manifest.seal_binding AND L10.fact); census+twelve-leap 9 green (no new DECISION producer); verdict-path gate 81 green; demo exit 0 in BOTH envs (dev cryptography 46.0.4 → lowered_to_abstain; fresh venv 48.0.1 → durable_not_lowered, backend 'pyca-cryptography-native'). **Named break (accepted):** manifests sealed before this change lack `maturity_outcome` and fail the new validator — none are in circulation (capstone landed 2026-06-12, zero production deployments). **Reported, not fixed (L10 track):** `assess()` keys on `ml_dsa.active_backend_id()` (capability) while the live sealing path stays ECDSA-P256 (use) — under cryptography>=48 a pq_non_repudiation claim PERMITs with no PQ signature anywhere on the decision; whether claim_honored should require the signer to actually be wired is an L10 contract decision. | **0 lines** — engine/pdp.py/main.py/pqcrypto untouched (capstone module + tests + demo script only) |
| onramp | track/onramp | ON-RAMP (developer first-run + public front door; **zero engine behavior change**) | **MERGED → main (2026-06-12; integration thread, founder-directed land — rebased onto the landed capstone-pqfix [f3d23bc atop b546073], COORDINATION collision resolved keeping both rows; README known-issue block on the capstone demo DELETED [the bug it described is fixed on main — demo exit 0 under cryptography 46 AND 48]; re-earned post-rebase: quickstart exit 0, verify_it_yourself exit 0, full-suite failed-set == baseline; README honesty re-check passed: no <1ms claim [measured 17.8/5.3 ms shown], no un-rerun ASR numbers, counterfactuals scoped to findings, ECDSA-P256-today/ZK-stand-in/certified=False/zero-production-deployments all stated)**: root `README.md` (public front door — every capability sentence RAN-this-session or maturity-labeled; ECDSA-P256-today / ZK-stand-in / certified=False / zero-production-deploys stated plainly), `scripts/quickstart.py` (zero-config first verdict: BEC wire-transfer → FORBID via the `monetary_transfer` structural floor + clean PERMIT contrast + replay-from-record check; exit 0 iff all three hold; no PYTHONPATH/server/Postgres/key — anonymous dev posture, `auth.py` untouched), `scripts/verify_it_yourself.py` (sys.path-bootstrap wrapper → `replay_trial_demo.py`, `--capstone` → `capstone_demo.py`; other tracks' demo files untouched), `scripts/smoke_guardrail.py` self-bootstrap fix `parents[0]`→`parents[1]`, `sdks/python/README.md` honesty pass (PyPI-404 + dead-`api.tex.systems` status banner, both re-verified 2026-06-12; SOC2/FINRA/HIPAA suitability claim cut), `CURATION.md` (4 `tex-frontend*.zip` untracked + gitignored after a clean secret-scan; full internal-docs move proposal with dependency notes left for founder at land). **Earned (fresh venv, scrubbed env, macOS arm64 py3.12):** `pip install -r requirements.txt` exit 0 in 16.4s (blake3/psycopg prebuilt wheels — no minimal-subset file needed); `quickstart.py` exit 0; `verify_it_yourself.py` (replay trial) exit 0; fixed smoke exit 0 (no PYTHONPATH); `test_structural_floor`+`test_crc_gate` 34 passed (README cites them). **Findings (reported, not fixed — not this track's files):** (1) `capstone_demo.py` exits 1 on fresh installs — pip now resolves cryptography 48.0.1 ⇒ `pq_durability.probe_backend()`='durable' ⇒ the PQ-claim stays PERMIT ⇒ env-pinned `assert ABSTAIN` at `capstone/flow.py:285` fails (engine correct in BOTH envs; README carries the known-issue note; fix chip spawned for the capstone track); (2) retired v1 seal key still in PUBLIC history (`eef9c24`→`6af43d0`, digest≠live key — rotation held, scrub deferred) — re-flagged URGENT in CURATION.md §0 before launch traffic; (3) no root LICENSE. | **0 lines** — `src/tex/engine/**`, `main.py`, all crypto/evidence untouched (diff vs origin/main = docs/scripts/curation only) |
| w3-l10-pqsign | track/w3-l10-pqsign | L10 follow-up (durable PQ backend live) | **COMMITTED, not merged (2026-06-14)**: makes the live env PQ-durable so L10's `pq_signing` works at runtime instead of lowering to ABSTAIN. Installed + pinned `cryptography>=48.0.0` (requirements.txt; resolves 49.0.0) ⇒ native ML-DSA (FIPS 204) via OpenSSL 3.5.5 ⇒ `ml_dsa.active_backend_id()`=`'pyca-cryptography-native'` ⇒ `probe_backend()`=DURABLE (id already allow-listed — **no allow-list change**, fail-closed list intact); also installed the declared-but-absent `blake3` (requirements.txt:13). **Durable label earned by a WORKING BODY (not a resolving name — the nanozk-in-reverse trap):** ML-DSA-44/65/87 keygen→sign→verify round-trips green at exact FIPS-204 sizes (pk 1312/1952/2592, sig 2420/3309/4627; seed=32B ⇒ `from_seed_bytes`), 1-bit-flip + wrong-msg fail; a PQ-non-repudiation claim is HONORED end-to-end through the live PDP (PERMIT, no flag, no sealed fact). Demo L10 line `lowered_to_abstain (backend None)` → **`durable_not_lowered (backend 'pyca-cryptography-native')`**, `capstone_demo.py` exit 0, honest split keeps `L10.pq_signing` runtime_dependent. **CONTRACT DECISION — resolves the capstone-pqfix "Reported, not fixed (L10 track)" item:** `claim_honored`/DURABLE means a production-durable ML-DSA backend is **available to the signer** (capability), NOT that this epoch's evidence chain is PQ-signed — the live ledger signer stays **ECDSA-P256** (`provenance/ledger.py`). Carried verbatim in `pq_durability.py`'s new "capability-vs-use line" docstring + the new durable e2e test; wiring the live ledger to emit an ML-DSA/composite signature is the separate larger step this leap does NOT take. **Tests:** named-3 green (104; lowering-branch tests now force `active_backend_id→None` to pin that branch deterministically [fail-if-broken preserved], `test_live_probe_is_none_in_this_env`→`..._durable_...` asserts the requirements-pinned durable env, nanozk-trap test isolates via monkeypatch, +1 live durable-honored e2e; `test_backend_probe.py:521` compares to `baseline.available` not hardcoded False). **Cross-track env-fragility fixed (the SAME latent bug capstone-pqfix noted — fails on ANY fresh `pip install`):** 4 files asserting the PQ claim lowers (`test_decision_fact_contract`, `test_wave2_twelveleap_composition`, `test_attempt_seal`, `test_wave2_eightleap_integration`) made env-robust via the established monkeypatch-None pattern (module autouse fixture / per-test) ⇒ 9 failures → 0. **Evidence:** PQ surface 276p/98s/**11f** (cryptography 46, no blake3) → **334p/52s/0f** (49+blake3); broad crypto sweep 635 passed; 0 new failures (the 11 blake3 + 1 surfaced institutional case were one missing-declared-dep, resolved). `research-early` (the signal is live + replayable; the live chain is not yet PQ-signed — that gap is now documented, not hidden). | **0 lines** — engine/pdp.py/main.py untouched (the `apply_pq_durability_hold` wiring already landed with wave2-pqlive); dependency + docstring + test changes only |

## Wave 3 status table (each W3 thread updates its row)
| Track | Branch | Leap | Status | Touches pdp.py/main.py? |
|---|---|---|---|---|
| w3-l1-zkproof | track/w3-l1-zkproof | L1 (real ZK backend) | **BUILT on track/w3-l1-zkproof (review-pending, NOT merged; 2026-06-14)** (`research-early`): first **wired + runnable, non-shim** L1 backend, replacing the keyed-hash stand-in for the fuse path. `zkprov/schnorr_group.py` (Pedersen over RFC-3526 Group-14 2048-bit safe prime — primality re-checked live with `sympy.isprime`, never trusted; 128-bit Fiat–Shamir; CDS OR-proofs; bit-range proofs; fixed-base comb pinned bit-identical to `pow`) + `zkprov/zk_fuse.py` (proves `fused_q == clamp(round(Σwᵢsᵢ/scale))` over PRIVATE in-range scores — hiding, sound under DLog, offline, **no SRS / no enclave / no chain**). Backend `schnorr-fuse-zk-v1` registered (id + class + dispatcher + `_REGULATOR_GRADE` non-shim *tier*, honesty-caveated). **Arbiter needed 0 logic changes** — a non-shim backend has `stand_in=False`, bypasses the `TEX_ZKPDP_ALLOW_SHIM` hard gate naturally → `verify_arbitration` reports `stand_in=False`+`regulator_grade=True`, so **L1 reaches `green` without the shim flag** (proven). **Earned:** group primality live-verified; completeness (mid/low/high/zero-weight); soundness (every forged `fused_q` rejected, prover refuses false statements, tamper rejected); **hiding** (distinct witnesses → same `fused_q` → both verify, commitments differ); arbiter green-without-flag + shim hard-gate still intact + router-skipped refused-not-faked. 26 new tests + existing 29 arbiter + 102 zkprov green; tests/capstone 51 green; **demo exit 0**. **Honest gaps (NOTES.md):** hand-rolled/UNAUDITED, pre-quantum (2048-bit DLog), NOT succinct (Σ-protocol, ~100s KB), proves the FUSE kernel only (threshold/floor/pin/chain stay public). **Capstone stays `green_test_mode` — characterized, not faked:** its headline decision is a *structural-floor FORBID* (router-skipped, no fuse → backend refuses), AND its bundle publishes raw scores (hiding not realized); a genuine capstone `green` needs a fuse-path decision + a hiding `ArbitrationStatement` (omit scores) + a consistency rewrite of the L1 validator (preserves the over-claim pins) — scoped in NOTES, deliberately not rushed. The succinct/audited path (ezkl/Halo2) stays RUNTIME-DEPENDENT/`BackendUnavailable`. | **0 lines** — engine/pdp.py/main.py untouched (zkprov/ + zkpdp/ docstrings only) |
| w3-l2-tee | track/w3-l2-attestation | L2 (off green_test_mode) | **BUILT — commit-pending, NOT merged** (`research-early`): L2's composite attestation moved off the test-mode `alg=none` JWT to a **real signed JWS** (default **PS384**, Intel Trust Authority's production algorithm). The capstone's L2 now reaches `green` via the real signature path with **no `alg=none` bypass** — the existing verifier (`verify_attestation`/`_verify_signature`) was **UNCHANGED** (it already accepted PS384/RS256/ES256/ES384 + ML-DSA/hybrid). New: `build_signed_composite_jwt` + `generate_standin_ita_keypair` + `_sign_signing_input` (`tee/attestation_client.py`, signer symmetric with the existing verifier; shared `_composite_claims` keeps the test-mode JWT byte-identical); `build_verdict_bound_signed_jwt` + `VerdictBindingResult.{signature_alg,signature_verified}` (`tee/verdict_binding.py`, `signature_verified` re-checked directly so it is posture-independent). Capstone wiring: compose generates a stand-in ITA keypair, signs the L2 token, and verifies it under a **production posture** (test mode popped) against the pinned key — fail-closed; the stand-in **public** key is pinned out-of-band (`CapstonePins.ita_public_key_pem` + sealed `PinDigests.ita_public_key_pem_sha256` + `pins.digest` cross-check), exactly like the ledger/evidence/voice/log keys. L2 status `green` with machine-readable halves `{signature: green, hardware_measurement: runtime_dependent}`; the manifest L2 honesty pin was rewritten — `green` is **unconstructible** unless `signature_verified=True` ∧ `test_mode=False` ∧ `alg != none` ∧ the hardware half stays `runtime_dependent`. `flow.py`/`capstone_demo.py` no longer set `TEX_TEE_ATTESTATION_MODE=test` (offline verification + tamper matrix run with NO test mode). **Honest residual (labeled in NOTES.md, docstrings, manifest caveats, demo):** the signature proves authorship by the pinned key — Intel's ITA key in production, a **stand-in** offline — and the **hardware-rooted measurement (MRTD/RTMR + a TDX quote that signs `report_data`) stays RUNTIME-DEPENDENT** on a real Intel TDX confidential VM (measurements are dev-stub off real hardware). The test-mode path is KEPT (`build_test_mode_composite_jwt`/`build_verdict_bound_test_jwt`/the `alg=none` verifier gate); the capstone simply no longer depends on it. **Earned:** demo `scripts/capstone_demo.py` exit 0 (34/34 offline checks; **12-row** tamper matrix incl. the new `forged L2 attestation signature` row caught by `L2.signature_invalid` — proving the signature is load-bearing); `tests/tee/` + `tests/frontier_thread_12_tee/` 172 green; `tests/capstone/` 56 green (new `test_l2_fails_closed_on_wrong_ita_key` + 3 unconstructibility pins: hardware-promoted / alg-none / signature-unverified); signed path round-trips PS384/RS256/ES256/ES384 and fails closed on wrong-key / no-key / tampered-signature. Full suite **5028 passed / 24 failed** vs stashed baseline 5022 / 25 — **new-failure set EMPTY** (24 ⊆ 25 pre-existing; the 1 delta is a governance-route flake that fails in baseline and passed here; +6 passed = the added tests). Pre-existing failures are the env-dependent PQ-durable-backend set on `cryptography 49` + API-route flakes — none in `tee/` or `capstone/`. | **0 lines** — `engine/pdp.py`/`main.py`/crypto untouched (`tee/` + `capstone/` + demo + tests only) |

## Batch-3 cross-leap findings (recorded 2026-06-11; first tree where all twelve leaps' artifacts coexist)
Eight-leap e2e re-run green on the merged tree (4 passed); 5-lens adversarial review (per-leap traps + cross-leap): 0 blockers. Three forward-looking cautions — none live today (each gated on a switch that is off), all owed to the seam track before the relevant switch flips:
1. **Shared-ledger DECISION disambiguation is by accident, not contract.** L10 seals a second `SealedFactKind.DECISION` per request (`pq_durability.py:338`, appended during routing at `pdp.py:400-404`) before M0's decision seal (`pdp.py:490`). L1's `check_seal_binding` survives only via `matching[-1]` ordering (`zkpdp/arbiter.py:1026-1037`); L3's count-conservation survives only because PQ `seal_detail()` happens to omit a `verdict` key (`negative_knowledge.py:598-608`). **ADDRESSED by `tests/test_decision_fact_contract.py` (1395d7a, landed 2026-06-11, mutation-verified at land time):** construction census over src/tex (a new DECISION-kind producer fails the census until enumerated with its verdict-key contract declared), exact detail-key-set pins for both producers, and live-flow append-order + `matching[-1]` resolution pins; `tests/test_wave2_twelveleap_composition.py` additionally pins the resolution amid the PQ/reflexive/drift interleave. This is a pin, not a fix — the stronger future fix remains introducing a distinct kind (same `models.py` precedent as the proposed `ATTEMPT`), a seam-track decision.
2. **An L5-bound process degrades first-boot key durability.** Metaguard cautions every KEY_MUTATION (`metaguard.py:312-321`) → PERMIT→ABSTAIN → `_persist_key` denied (`evidence/seal.py:192-194`) → L7 campaign seals and M0b corpus provenance in that process sign with a never-persisted key; M0b PINNED-authorship verification fails after restart. Reachable only via programmatic `bind_reflexive_governor` (no env flag, not main.py-wired). Document in the L5 binding runbook when composition-root wiring lands; existing key files load normally. Re-verified still-not-live 2026-06-11 (no `bind_reflexive_governor` call site outside selfgov/ and tests; not main.py-wired) — no code change owed now; the obligation transfers to the future L5 composition-root binding PR's runbook and survives here until that PR cites it.
3. **The L3 attempt hook must scope its counts.** Once the seam-track attempt hook lands, L5 reflexive gate evals (policy `reflexive-governor`, `governor.py:594-601`) and any non-customer DECISION traffic inflate `n_attempts`/verdict counts → spurious GATED-BROKEN. **DECIDED at hook landing 2026-06-11 (track/wave2-attempthook): gate evaluations COUNT as attempts** — the identity stays global and symmetric (reflexive eval = 1 ATTEMPT + 1 verdict-keyed DECISION, balances; fast-paths produce neither fact); the action_type-filter alternative was rejected (byte-identical dual-filter divergence trap + hides governance mutations from conservation). Declared in `provenance/attempt_seal.py`'s contract; `tests/test_wave2_twelveleap_composition.py` recomposed consciously (a customer-only count now fires the contradiction alarm against the sealed ATTEMPT facts, by construction).
Also noted (no action owed): L6 checkpoints cover ALL ledger kinds (`gix.py:682-684`) — never cite `tree_size` as "number of decisions"; the eight-leap e2e should become a twelve-leap composition test (NEW test pinning L5-bound + L6 heterogeneous-ledger consistency + L3 cert over the same epoch; M0b stays bench-only) — delivered as `tests/test_wave2_twelveleap_composition.py` on track/wave2-twelveleap-e2e (also pins cautions 1–3 as live asserts: M0-fact resolution amid the PQ/reflexive interleave, and customer-only `n_attempts` ⇒ GATED-BROKEN).

## Citation discipline (Wave 2)
Citations in `ROADMAP.md` were retrieved by the `tex-wave2-mythos` workflow on 2026-06-10. The adversarial judge flagged a
handful of designer-supplied ePrint IDs as misattributed and a few IETF drafts as `UNVERIFIED-FROM-MEMORY` (carried from
`tee/` docstrings). **Before any citation lands in code, a docstring, or a customer-facing claim, the owning track
re-verifies it against the primary source.** No Wave-2 *capability* claim rests on a citation — only on a green benchmark.

## Wave 3 status (each thread updates its row)
| thread | branch | leap | status & evidence | pdp.py/main.py lines |
|---|---|---|---|---|
| moat-external-anchor | track/moat-external-anchor | Moat: external proof-of-age | **COMMITTED on branch, NOT merged** (`research-early`): closes the "chain proves order, not time" gap — `record_hash` binds no external timestamp, so a key-holder could mint a multi-year chain today. New, **additive** files only: `interchange/external_anchor.py` (RFC-3161 request builder + `CheckpointAnchorRecord` + the load-bearing offline verifier `verify_anchor_receipt` — real CMS-signature verification of the TSA token against a **pinned** cert + `id-kp-timeStamping` EKU + leaf→CA chain + messageImprint == digest recomputed from the gix tree-head fields, never trusted from storage); `interchange/_local_tsa.py` (self-issued RFC-3161 TSA for offline demo/test — labeled "proves nothing about real time"); `scripts/anchor_checkpoint.py` (daily job: timeout-bounded+retrying httpx poster, verify-before-persist, publishes the tree-head; gated by `TEX_EVIDENCE_ANCHOR_ENABLE`, dev stays offline); `scripts/anchor_demo.py` + `verify_it_yourself.py --anchor`; `.github/workflows/anchor.yml` (daily cron → freetsa → commit the public tree-head = second channel); `anchors/tsa/freetsa_cacert.pem` + `PIN_STATEMENT.md` (out-of-band pin, DER sha256 `a6379e7c…`) + `anchors/README.md`; `tests/test_external_anchor.py` (16). **Deliberately does NOT reuse `c2pa/timestamp.py`** (its `parse_and_validate_response` never verifies the TSA CMS signature — for the moat that signature IS the proof). **Earned:** 16 anchor tests green (genuine verifies + 9 fail-closed tamper/forgery/pin/EKU/not-granted cases, all real-CMS not mocked) + the 4 keep-green suites + the 5 guard suites = **142 passed, 0 failures**; **real freetsa.org interop VERIFIED this session** (a 4641-byte token → `ok=True`, genTime 2026-06-17T15:40:50Z, "independent of Tex's key") and the **committed** receipt re-verifies offline against the **committed** pin (court-exhibit: a fresh clone proves it without trusting Tex); additive-invariant test asserts anchoring leaves `verify_evidence_chain` + the merkle root byte-identical; verify path imports without `httpx` (isolation preserved). Frontier-checked: pyca has no native RFC-3161 verify ⇒ hand-rolled CMS via `pyasn1_modules.rfc5652`; OTS/Rekor structured as future alternate backends behind the same interface. | **0 lines** (pdp.py/main.py untouched; `interchange/gix.py`/`evidence/chain.py`/ledger records unmodified — anchoring binds a receipt ALONGSIDE the chain, never inside it) |
| w3-l11-entailment | track/w3-l11-entailment | L11 (entailment half) | **COMMITTED on branch, NOT merged** (`research-early`): built the green-ready machinery for the L11 entailment half; the LIVE half stays **BLOCKED** — honestly — because `import transformers` raises in-env (`tokenizers==0.21.2` vs the `<0.14` pin, verified 2026-06-14) and no FIELD NLI corpus exists. Applies the ESTABLISHED method (Mohri–Hashimoto, "Language Models with Conformal Factuality Guarantees", ICML 2024, [arXiv:2402.10978](https://arxiv.org/abs/2402.10978), re-fetched 2026-06-14) — not a Tex novelty. `voice/voice_gate.py`: real transformers cross-encoder `NeuralNLIScorer.score()` (softmax P(entail), entailment index read from `config.label2id`, never hard-coded) + fail-closed `load()`/`entails()` (None here — M0c probe + `test_neural_scorer_is_off_and_honest` stay green) + `conformal_lambda_hat()` (the `⌈(n+1)(1-α)⌉` order statistic, pure-python) + `calibrate()` (nonconformity = the back-off to drop a not-entailed claim; `model_loaded` True only for the neural backend). `voice/entailment_cert.py`: schema **v2** — `lambda_hat:float`/`calibrated:bool` constructible only as a COHERENT block (calibrated ⟺ λ̂ ⟺ backend ⟺ α ⟺ n ⟺ named corpus; `model_loaded ⟺ neural`; λ̂∈[0,1]); **load-bearing pin: `"field"` kind unconstructible without a loaded neural calibration** (a stub/synthetic λ̂ self-labels and can never earn field); `entailment_half_status()` is the ONE shared green rule. `capstone/{compose,manifest,verify}.py`: the L11 entailment half is DERIVED from the commitment (live = absence ⟹ blocked); manifest validator became a coherence gate (blocked⟹absence fields; green⟹real field calibration); offline verifier independently RE-DERIVES the half from the chain-sealed commitment bytes and cross-checks the manifest claim (green not forgeable above the seal). **Earned:** `tests/voice/` + `tests/capstone/` 115 green; new `tests/voice/test_voice_conformal.py` (8: conformal math hand-checked + end-to-end calibration over the REAL synthetic corpus with a deterministic, label-blind stub; calibrated payload vocabulary-clean); capstone honesty pins extended (coherent field-green VALIDATES; incoherent/stub "green" REFUSED); `scripts/capstone_demo.py` **exit 0** (prints `blocked: L11.entailment`, 11/11 tamper rows caught). Full suite **5036 passed / 25 failed**; failed-set diff vs baseline (changes stashed) = **EMPTY** (identical 25 — all from cryptography≥48's native ML-DSA backend flipping the PQ branch + pre-existing route/DB infra; none in voice/ or capstone/). **Residual (documented, NOTES.md):** schema enforces coherence not truth — a key-holder can sign a coherent-but-fabricated green; attributable + tamper-evident, not impossible (same residual as every provenance system); the live `flow.py` builds from the live scorer (`model_loaded=False`) so the product never emits a false green, and an incoherent forge is caught at replay. **Flip-to-green path (NOTES.md):** working transformers + a pinned field corpus ⇒ `calibrate(real_scorer, field_pairs)` → `commitment_from_calibration(..., corpus_kind="field")` → seal; compose derives green, no further code change. | **0 lines** (pdp.py/main.py/voice_ask.py/attestation.py untouched; voice_gate.py extended additively) |
| presence-s2-gate | presence/s2-gate | Presence S2 (the truth-gate) | **BUILT on branch, NOT merged** (`research-solid`): the deterministic truth-gate that turns each `PresenceClaim` into one monotone `PresenceVerdict`. New, self-contained pkg `presence/gate/` (`gate.py`/`queries.py`/`conformal.py`/`evidence.py`/`compose.py`/`telemetry.py`/`redteam.py`) — imports the **frozen** `tex.presence.contract`, never edits it. **Recompute authority:** AGGREGATE values are re-derived from rows (`decision_store.find`, `agent_registry.list_all`, `action_ledger.total_count`, `connector_health_store`, `discovery_ledger`, `scan_run_store`) and bound to `recomputed_value`; the model's number is never trusted. **Real EvidenceRefs:** a `Decision`/`DiscoveryLedgerEntry` binds its own sealed anchor (`content_sha256` / chain `record_hash`+`previous_hash`); rows without one bind a recomputable `sha256(canonical_json(row))` (same `_stable_json` idiom as `discovery_ledger`). **DERIVED:** root-cause localization via the existing `causal/conformal_attribution.ConformalPredictionSet`; `correctness_floor=target_coverage`, `coverage_mode` read from the set (transductive≈approx; `calibrated` only with `TEX_CONFORMAL_CALIBRATION_PATH`) — never claims calibrated while transductive. **Threat model proven:** the draft has NO vote on tier — base tier is a pure function of (recompute grounded ∧ evidence present); a draft value-contradiction can only LOWER to ABSTAIN (monotone, `tighten`), never raise. The gate AUTHORS the spoken phrasing of the recomputed value, so injected words never reach the user. Contract invariant held: **evidence empty iff ABSTAIN** ⇒ a true zero-count ABSTAINS honestly (no completeness proof for absence). **Seam:** `voice_ask.py` runs the gate in PARALLEL to VoiceGate (does not replace the sealed-fact floor); `AskOutcome` gains optional `presence: AnswerEnvelope|None`; INERT by default (NULL_BRAIN proposes nothing ⇒ `presence=None` ⇒ live answer byte-identical). Answer-level ABSTAIN raises one `dimension="presence"` `HeldDecision` into the existing `HeldDecisionSink` (no new route). **Red-team:** 9-case held adversarial corpus (false number / injection / fabricated tier / unknown / nonexistent target / status-contradiction) — every case is abstain-or-exact-grounding; lie-test asserts no supported verdict diverges from an independent recompute and no injection marker leaks to speech. Telemetry: abstain / grounding / recompute-mismatch rates. **Honest edge:** "cannot lie" = honest abstention + a conformal floor under exchangeability — never absolute. Tenant scope disclosed, not faked: agent_count is tenant-filtered (AgentIdentity has tenant_id); decision/action aggregates are GLOBAL (no tenant column upstream) — documented in the module banner, not papered over. **Earned:** new `tests/presence/` 35 green (recompute authority, monotonicity battery under hostile drafts, conformal honesty incl. calibrated-vs-transductive, envelope stripping + held-decision, red-team, seam integration). Full suite (`PYTHONPATH=src pytest`, zkprov/sympy collection-skip) **5739 passed / 73 skipped / 3 failed in 7:00**; the 3 failures (`test_governance_history_routes.py`) reproduce identically with my edit **stashed** on a clean tree and reference neither presence nor voice_ask ⇒ **pre-existing, 0 new failures**. | **0 lines on pdp.py/main.py**; `voice_ask.py` extended additively (import + module gate/telemetry + `_run_presence` + optional `AskOutcome.presence` + 3 dimension-branch lines) |
