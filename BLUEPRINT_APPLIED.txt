TEX BLUEPRINT — APPLIED TO THIS REPO
=====================================
This is the original Tex repository with the code-derived blueprint added and the
inaccurate prior-claim doc artifacts removed. The source code is UNCHANGED.
Single source of truth for what the system does: TEX_SYSTEM.md.

WHAT WAS REMOVED (inaccurate / superseded claim artifacts)
  - audit/                      prior audit registry; its numbers DISAGREE with code
                                (fossil claimed 462 files / 377 wired / 29 orphans and
                                 "enforcement + path_policy never wired"; code-derived
                                 truth: 524 files / 439 wired / 0 isolated, and those
                                 modules ARE wired via /v1/govern/decide).
  - docs/                       prior hand-authored layer docs (LAYER_*.md,
                                CROSS_CUTTING_*.md, BEHAVIORAL_PROVENANCE.md),
                                superseded by TEX_SYSTEM.md sections 4-9.
  - .pytest_cache/, .DS_Store   build / OS cruft.

WHAT WAS *NOT* TOUCHED
  - src/tex/**  — every module is byte-for-byte identical to the original.
    ALL docstrings and comments are intact. Nothing in the source was stripped,
    rewritten, or deleted. (Inaccurate docstrings, e.g. the items in TEX_SYSTEM.md
    section 9, are still present in the code; they are documented in the ledger,
    not removed. Ask if you want a surgical pass that fixes only those.)
  - tests/, sdks/, pep/, scripts/, examples/, vendor/, tex-frontend/ — unchanged.
  - deploy/helm/tex/README.md, pep/kernel/README.md, sdks/python/README.md kept
    (verified consistent with the code-derived runtime topology).

BLUEPRINT ADDED AT ROOT
  - TEX_SYSTEM.md        the code-derived blueprint (12 sections + 524-row appendix)
  - THREAD_PRIMER.md     short orientation for future work
  - index.json           machine-readable map (re-derivable)
  - SELF_AUDIT.txt       Phase-9 invariants + coverage proof (524/524 read)
  - diagrams/            layer arch, package deps, decision path, runtime topology
  - audit_tools/         all 8 scripts — the blueprint regenerates from source
  - audit_tools/cleanup.sh  the deletion script (already applied here; kept for record)

GIT HISTORY NOTE
  .git is preserved — this is still your repo. The removed audit/ and docs/ trees
  still exist in history; the working tree is clean but history is not rewritten.
  Changes show as uncommitted working-tree edits: review with `git status`, commit
  when ready.
